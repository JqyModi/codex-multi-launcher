{
  "name": "codex-profile-manager",
  "version": "0.1.0",
  "private": true,
  "description": "macOS desktop utility for managing isolated Codex App profiles.",
  "main": "dist-electron/main/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run build:main && npm run build:renderer",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "tsc -p tsconfig.renderer.json && vite build",
    "dev": "npm run build:main && concurrently -k \"tsc -p tsconfig.main.json --watch\" \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build:icon": "node scripts/build-icon.mjs",
    "package:mac": "npm run build:icon && npm run build && electron-builder --mac dir --arm64 --config electron-builder.yml",
    "start": "npm run build && electron .",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "verify:e2e": "npm run build && node scripts/verify-profile-generation.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "lucide-react": "^0.561.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "typescript": "^5.9.3",
    "vite": "^7.2.7",
    "wait-on": "^9.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "electron-builder": "^26.8.1",
    "electron": "^39.2.7"
  },
  "author": "Modi"
}
